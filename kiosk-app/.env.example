VITE_API_BASE_URL=http://localhost:5000/api/v1
VITE_SCALE_ID=SCALE_01
# Provisioned by the backend. Treat this as a device credential, not a public web key.
VITE_DEVICE_TOKEN=<device-uuid>.<random-secret>
